import React from "react";

const NotFound = () => (
  <div style={{ textAlign: "center", marginTop: 80 }}>
    <h2>404 - Page Not Found</h2>
    <p>That page doesn't exist. Check the URL or use the navigation links above.</p>
  </div>
);

export default NotFound;